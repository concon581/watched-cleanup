services:
  watched-cleanup:
    build: .
    container_name: watched-cleanup
    user: "992:114"
    ports:
      - "6969:6969"
    volumes:
      - /data:/data
    environment:
      - JELLYFIN_BASE_URL=${JELLYFIN_BASE_URL}
      - JELLYFIN_API_KEY=${JELLYFIN_API_KEY}
      - JELLYFIN_USER_ID=${JELLYFIN_USER_ID}
      - RADARR_BASE_URL=${RADARR_BASE_URL}
      - RADARR_API_KEY=${RADARR_API_KEY}
      - SONARR_BASE_URL=${SONARR_BASE_URL}
      - SONARR_API_KEY=${SONARR_API_KEY}
      - DATA_PATH=${DATA_PATH:-/data}
      - TORRENTS_PATH=${TORRENTS_PATH:-/data/torrents}
      - DRY_RUN_MODE=${DRY_RUN_MODE:-true}
      - WEB_USERNAME=${WEB_USERNAME:-admin}
      - WEB_PASSWORD=${WEB_PASSWORD}
    # Give an in-flight deletion time to finish before the container is killed
    stop_grace_period: 4m30s
    restart: unless-stopped
